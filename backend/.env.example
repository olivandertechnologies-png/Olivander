BACKEND_ORIGIN=https://olivander.onrender.com
XERO_CLIENT_ID=
XERO_CLIENT_SECRET=
XERO_REDIRECT_URI=http://localhost:8000/auth/xero/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback
FRONTEND_ORIGIN=http://localhost:5173
SUPABASE_URL=
SUPABASE_KEY=
# Service-role key — bypasses RLS. Required.
SUPABASE_SERVICE_KEY=
GROQ_API_KEY=
JWT_SECRET=
# Generate ENCRYPTION_KEY with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=
# Generate WEBHOOK_SECRET with:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
WEBHOOK_SECRET=
# Gmail Pub/Sub topic used to register inbox watches after Google OAuth.
PUBSUB_TOPIC=projects/olivandertechnologies/topics/gmail-watch
